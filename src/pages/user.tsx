import Layout from '@theme/Layout';
import User from '@site/src/modules/user';

const Page = () => {
    return (
        <Layout title="用户权限">
            <User />
        </Layout>
    );
};

export default Page;
