import RegisterPage from '@site/src/modules/user/register';
import Layout from '@theme/Layout';

const Page = () => {
    return (
        <Layout title="登记用户">
            <RegisterPage />
        </Layout>
    );
};

export default Page;
