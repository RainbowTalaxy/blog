import Layout from '@theme/Layout';
import TokenGenerate from '@site/src/modules/user/token';

const Page = () => {
    return (
        <Layout title="Token 生成">
            <TokenGenerate />
        </Layout>
    );
};

export default Page;
