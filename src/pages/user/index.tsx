import Layout from '@theme/Layout';
import UserPage from '@site/src/modules/user';

const Page = () => {
    return (
        <Layout title="用户权限">
            <UserPage />
        </Layout>
    );
};

export default Page;
